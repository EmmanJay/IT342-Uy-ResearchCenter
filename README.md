# IT342-Uy-ResearchCenter

Note: For local development, this repository includes committed `.env` file(s) (for example, `web/.env` and/or `backend/.env`) that contain API keys and other configuration required to run the project locally. Keep these files in place when running the project; remove or rotate any sensitive values before publishing the repository publicly.